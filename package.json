{
  "name": "moodush-extension",
  "version": "2.9.4",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:config": "node scripts/generate-build-config.mjs",
    "build:extension": "npm run build:config && node scripts/build-extension.mjs",
    "release:manifest": "node scripts/sign-release-manifest.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
